{
  "name": "convert_currency",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "concurrently \"npm run serve --prefix client\" \"npm run server --prefix server\"",
    "setup": "yarn && cd client && yarn && cd .. && cd server && yarn && cd .."
  },
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
